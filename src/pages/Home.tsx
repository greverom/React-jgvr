
import { HomePageContainer, HomeTitle } from "../styles/styled/homePage.style"; 

const Home= () => {
    return (
        <HomePageContainer>
            <HomeTitle>Home Page</HomeTitle>
        </HomePageContainer>
    );
};

export default Home;