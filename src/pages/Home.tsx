
import { HomePageContainer, HomeTitle } from "../styles/Home/homePage.style"; 

const Home= () => {
    return (
        <HomePageContainer>
            <HomeTitle>Home Page</HomeTitle>
        </HomePageContainer>
    );
};

export default Home;