import { LoadingContainer, Spinner } from "../../styles/styled/loading.style";

const Loading = () => {
  return (
    <LoadingContainer>
      <Spinner></Spinner>
    </LoadingContainer>
  );
};

export default Loading;