import { LoadingContainer, Spinner } from "../../styles/Loading/loading.style";

const Loading = () => {
  return (
    <LoadingContainer>
      <Spinner></Spinner>
    </LoadingContainer>
  );
};

export default Loading;